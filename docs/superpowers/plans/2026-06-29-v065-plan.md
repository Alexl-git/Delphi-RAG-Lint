# v0.65 planning doc (scope + sequencing)

> **Status:** ROADMAP-LEVEL plan. On resume: brainstorm the status-bar feature, then use
> superpowers:writing-plans to produce a DETAILED per-wave plan, then
> superpowers:subagent-driven-development to execute. Keep `tests/lint/run_lint_tests.ps1` green.
> **Current baseline:** v0.64.1-alpha RELEASED, origin/main = 2188eed, harness 78/78.

**Goal:** Ship v0.65 covering (A) the remaining field-report false positives, (B) the R2 IDE job
queue + a status/progress bar in the drag-lint dock window, and (C) indexer/grep-elimination quick
wins + recorded minor follow-ups. User wants the backlog AND the planned work released in v0.65.

---

## Stream A -- FP backlog (SMALL: only FP-8 + FP-9 remain)
VERIFIED 2026-06-29: FP-1..FP-7 from `C:/Projects/DB/ORM3/CLIENT/LINT-FALSE-POSITIVES-20260628.md`
are ALREADY FIXED in main (FP-1 in v0.64; FP-2/3 a7e2945, FP-4 7600daf, FP-5/6 a6fd393, FP-7 6f7603a;
fixtures present). Only these remain:
- **FP-8 `unit-not-in-dpr`** misparses the `.dpr` uses clause: ignore `{form hints}` and `{$directives}`;
  only treat `Ident` / `Ident in 'file.pas'` as units; optionally suppress EurekaLog `E*`/`ExceptionLog*`.
- **FP-9 `unit-not-in-project`** over-broad + misses dotted names: match the FULL dotted identifier
  (case-insensitive) so `Foo.ViewModel` matches its `in 'Foo.ViewModel.pas'` / `Include=`; treat
  installed-package/library-path units, `COMMON\OBJECTS\` generated units, and `*_SERVER` units as
  out-of-(this)-project-by-design (don't flag). Genuinely-actionable set is then a tiny handful.
These are project-membership rules (likely in ProjectChecks/ProjectRules, NOT the per-file AST path);
each needs a fixture/project-fixture. Re-validate: after fixes, the 72+837 noise should collapse to a few.
First TASK on resume: locate where unit-not-in-dpr / unit-not-in-project are implemented (drag-lint query
the DragLint self-index AFTER reindexing it, or grep) and design the fix + fixtures.

## Stream B -- R2 IDE job queue + status/progress bar (the headline v0.65 IDE work)
R2 (from the roadmap): serialize heavy IDE jobs (reindex / lint-all / forms-csv / compile-check) so they
don't collide; keep LSP live. The status bar is the QUEUE'S UI -- build them together.

**Status-bar feature (user request) -- design sketch (BRAINSTORM + refine on resume):**
- WHERE: a status strip along the BOTTOM of the drag-lint dock window (`TDragLintDockForm`, the dedicated
  window with Structure / Search / Blast Radius tabs). A `TPanel`/`TStatusBar` docked alBottom, visible
  across all tabs.
- SHOWS: current running job name + a progress indicator (percent/marquee), queue depth ("N queued"),
  and last-completed result (e.g. "lint-all: 78/78, 0 errors"). Reuse the lint-all `[i/N] NN%` progress
  lines already streamed via RunCaptureStreaming (v0.64.1) to drive the bar for lint-all.
- DATA SOURCE: the R2 job queue publishes state (current job, queue list, progress%); the bar observes and
  updates on the MAIN THREAD (TThread.Queue/Synchronize -- OTAPI UI is main-thread-only). Build the queue
  first (or together) so the bar has a real model to show.
- OPEN QUESTIONS for brainstorm: click-to-expand the queue list? a Cancel button per job? show idle state?
  persist/restore across IDE restart? throttle update frequency? Does it replace or complement the Messages-
  view progress posts (avoid double-reporting)?
- Build-verified + manual IDE test (no OTAPI harness). Win32 BPL rebuild + deploy to third_party/dll-win32/,
  RAD Studio CLOSED.

## Stream C -- indexer/grep quick wins + recorded follow-ups
- **Grep-elimination wishlist** (`docs/superpowers/specs/2026-06-29-grep-elimination-indexer-wishlist.md`):
  pick P1 items that are feasible -- (1) working-tree/live query mode (re-parse dirty files), (2) regex
  `--text`, (3) index non-Delphi files (.json/.scm/.md) into the FTS corpus. Highest leverage = (1).
- **Minor follow-ups from the v0.64.1 final review** (low impact, tidy here):
  - `CheckSyntaxErrors` / `BuildConditionalRanges` still does one extra `TEncoding.UTF8.GetString(Src)`
    per file -- thread `SrcUp` in (per-error-node O(N) decode already removed).
  - `CheckSyntaxErrors` spanning-overlap check now tests straddling the whole combined range set vs ANY
    single range -- weaker for files with MULTIPLE SEPARATE conditional regions (nested is fine). Restore
    per-range straddle test if cheap.
- **M1 type resolver** (roadmap, larger): cross-unit type resolution would let `string-equality-comparison`
  become truly type-aware (replacing the v0.64.1 literal-operand heuristic). Likely its own wave; decide on resume.

---

## Sequencing / release shape (propose; refine on resume)
- **v0.65.0** = Stream A (FP-8/FP-9) + Stream C quick wins (the two FP-1 tidies + maybe grep P1 #1). CLI-side,
  harness-testable, fast. Ship.
- **v0.65.1** (or fold in) = Stream B (R2 job queue + status bar) -- IDE/BPL, manual-tested. Bigger.
- M1 type resolver = likely v0.66 unless small.
User said "release backlogged + planned in v0.65" -- so both A and B land under the v0.65 line (as .0/.1 is fine).

## Decisions locked (from roadmap + this session)
- FP policy: when unsure, do NOT report but keep the rule.
- Naming rules ON by default (roadmap); serialize heavy IDE jobs (R2).
- Prefer drag-lint indexer over Grep -- but REINDEX the DragLint self-index first (it goes stale during edits);
  see [[feedback-draglint-over-grep]]. Build Win64 CLI exe + Win32 BPL via the delphi-build skill.
- Alphas are flagged PRERELEASE; consequence: GitHub keeps the last non-prerelease (v0.63.0) as "Latest"
  (it refuses to mark a prerelease Latest). OPEN: user to decide if v0.64.1/v0.65 should claim "Latest"
  (would require un-prereleasing).

## RESUME procedure (next session)
1. Reindex the DragLint self-index (`drag-lint index C:\Projects\Delphi-RAG-lint --db ...Delphi-RAG-lint.sqlite`)
   so symbol/text queries reflect current code; prefer it over Grep.
2. Brainstorm the status-bar feature (superpowers:brainstorming) to settle the open questions above.
3. writing-plans -> detailed v0.65.0 plan (Stream A + C quick wins) -> subagent-driven-development.
4. Then v0.65.1 (Stream B). Watch for any second parallel runner mutating HEAD (lesson from the v0.64 collision).
