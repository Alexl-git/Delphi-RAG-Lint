# case-with-too-few-branches (#6, threshold=2): a case with fewer than 2 arms is
# clearer as an if. Fires on the single-branch case (line 6); NOT on the
# three-branch case (line 9). 'caseCase' children are counted (an 'else' is not one).
case-with-too-few-branches 6
!case-with-too-few-branches 9
