RED baseline, recorded by RUNNING THE SNAPSHOT ENGINE (extractor 1.8.0-alpha).
    1.8.0-alpha -> 5 FAIL (2 call_edges + 3 type_ancestors), controls PASS
    1.9.0-alpha -> all 11 PASS

CAVEAT ON THE OLD RUN, so it is not misread: the IMPOSTOR guard and
find-callers both PASS here vacuously -- nothing resolved at all, so nothing
could resolve WRONGLY, and find-callers matches by NAME rather than by
resolution. They only become load-bearing once edges exist, which is the
state the new engine is in.

MEMBER C NEEDED THREE LAYERS, not the two its plan described. With the parser
and ResolveAncestry halves alone, type_ancestors held a perfectly correct
alias row that NOTHING LOOKED UP: GetTypeCandidates filtered receiver-type
candidates to class/interface/record, so ResolveTypeNameToSymbol could not
turn TAliased into a symbol id in the first place. The intermediate state --
ancestors green, edges still red -- is recorded because it is the whole
reason the third layer was found rather than assumed.

call_edges
  [FAIL] HAZARD: alias-typed receiver resolves to the base method edges=0
  [FAIL] HAZARD: and it resolves with confidence certain confidence=
  [PASS] CONTROL: inheritance-typed receiver resolves edges=1  -- green TODAY; breaking this is a restructure, not a fix
  [PASS] CONTROL: and with confidence certain confidence=certain
  [PASS] CONTROL: a direct call on an unrelated class resolves edges=1

IMPOSTOR GUARD -- a fix must not resolve by name alone
  [PASS] the alias-typed call does NOT bind the impostor Ping edges=0

type_ancestors shape
  [FAIL] HAZARD: the alias has exactly ONE ancestor row rows=0
  [FAIL] HAZARD: at ordinal 0 ordinal=-1
  [FAIL] HAZARD: naming the alias target ancestor_name=
  [PASS] CONTROL: a real descendant still has its row rows=1  -- alias rows must be purely ADDITIVE

find-callers (user-facing; a NAME match, not a resolution)
  [PASS] find-callers Ping lists the alias-typed site 

FAIL
