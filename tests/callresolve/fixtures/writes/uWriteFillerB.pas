unit uWriteFillerB;

{ Filler for run_write_refs_bind.ps1: keeps one edited file under the third of
  the corpus past which the resolve pass stops scoping, so check 5 exercises
  the SCOPED shape. No writes on purpose -- check 4 pins the write count. }

interface

procedure FillB;

implementation

procedure FillB;
begin
end;

end.
